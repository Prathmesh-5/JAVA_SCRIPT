{
    "name": "Node.js",
    "image": "mcr.microsoft.com/devcontainers/javascript-node:0-18",

    // Automatically runs `npm install` after the container is created
    "postCreateCommand": "npm install && echo 'global.print = console.log;' >> /workspaces/JAVA_SCRIPT/utils.js",

    "customizations": {
        "vscode": {
            "extensions": [
                "esbenp.prettier-vscode",
                "vscode-icons-team.vscode-icons",
                "wayou.vscode-todo-highlight"
            ],
            "settings": {
                "editor.fontSize": 19,
                "terminal.integrated.fontSize": 19,
                "editor.wordWrap": "on"
            }
        }
    }

    // Uncomment to connect as root instead.
    // "remoteUser": "root"
}
